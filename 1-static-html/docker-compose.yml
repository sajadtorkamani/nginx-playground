services:
    nginx:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "8001:80"
        volumes:
            - .:/usr/share/nginx/html
            - ./nginx.conf:/etc/nginx/nginx.conf